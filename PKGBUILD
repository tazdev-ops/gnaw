[Desktop Entry]
Type=Application
Name=Gnaw
Comment=MASQUE wrapper + MiM + admin helper + GTK GUI
Exec=gnaw --gui
Icon=applications-internet
Terminal=false
Categories=Network;GTK;
Keywords=masque;proxy;socks;vpn;tun;
StartupNotify=true
